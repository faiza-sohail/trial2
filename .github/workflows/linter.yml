tages:
  - build
  - deploy

build_image:
  stage: build
  script:
    - docker build -t website .

deploy:
  stage: deploy
  script:
    - docker stop website|| true
    - docker rm website || true
    - docker run -d -p 8000:80 --name website website
